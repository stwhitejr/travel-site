import {Anton, Lato} from 'next/font/google';

export const anton = Anton({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-anton',
});

export const lato = Lato({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-lato',
});
